# SendGrid API Key for email notifications
SENDGRID_KEY=your_sendgrid_api_key_here

# Firebase Credentials (as JSON string for Vercel)
# Copy the contents of key.json and paste as a single-line JSON string, or use the format below:
# Either set FIREBASE_KEY as the full JSON, or set individual Firebase variables:
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_PRIVATE_KEY=your_firebase_private_key
FIREBASE_CLIENT_EMAIL=your_firebase_client_email

# Or use the complete Firebase config as a JSON string (URL encoded or escaped)
# FIREBASE_CONFIG={"type":"service_account","project_id":"..."}
