<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Notifications</title>
  <link rel="stylesheet" href="/css/styles.css"> <!-- your CSS file -->
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f5f5f5;
      padding: 20px;
    }
    .container {
      max-width: 800px;
      margin: auto;
      background: #fff;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
    }
    h1 {
      text-align: center;
      margin-bottom: 20px;
    }
    .notif {
      border-bottom: 1px solid #ddd;
      padding: 10px 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .notif:last-child {
      border-bottom: none;
    }
    .notif.read {
      background-color: #f0f0f0;
    }
    .notif p {
      margin: 0;
    }
    .notif-time {
      font-size: 0.8rem;
      color: #666;
    }
    .btn-read {
      background-color: #4CAF50;
      color: white;
      border: none;
      padding: 5px 10px;
      border-radius: 5px;
      cursor: pointer;
    }
    .btn-read:hover {
      background-color: #45a049;
    }
    a.home {
      display: inline-block;
      margin-bottom: 20px;
      text-decoration: none;
      color: #333;
    }
    a.home:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <a class="home" href="/don_home">&larr; Back to Home</a>
    <h1><%= name %>'s Notifications</h1>

    <% if (notifications.length === 0) { %>
      <p>No notifications yet.</p>
    <% } else { %>
      <% notifications.forEach(function(notif) { %>
        <div class="notif <%= notif.read ? 'read' : '' %>">
          <div>
            <p><%= notif.message %></p>
            <span class="notif-time"><%= notif.createdAt.toDate().toLocaleString() %></span>
          </div>
          <% if (!notif.read) { %>
            <form action="/notifications/read/<%= notif.id %>" method="POST">
              <button type="submit" class="btn-read">Mark as Read</button>
            </form>
          <% } %>
        </div>
      <% }); %>
    <% } %>
  </div>
</body>
</html>
