<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Forgot Password</title>
    <link rel="stylesheet" href="css/common.css" />
    <link rel="stylesheet" href="css/auth.css" />
    <style>
      /* Inline fallback styles for forgot-password page (mobile-first) */
      :root { box-sizing: border-box; }
      *, *:before, *:after { box-sizing: inherit; }

      .auth-wrapper { padding: 20px; min-height: 100vh; display:flex; align-items:center; justify-content:center; background: transparent; }
      .auth-card {
        width: 100%;
        max-width: 480px;
        margin: 24px auto;
        padding: 20px;
        border-radius: 12px;
        background: #fff;
        box-shadow: 0 6px 18px rgba(0,0,0,0.06);
      }
      .auth-title { font-size: 1.25rem; font-weight: 700; margin-bottom: 6px; }
      .auth-sub { color: #6b7280; margin-bottom: 14px; }
      .form-label { display:block; margin-bottom:8px; font-weight:500; }
      .form-input { width:100%; padding:10px 12px; border:1px solid #e5e7eb; border-radius:8px; font-size:1rem; }
      .auth-actions { display:flex; gap:12px; margin-top:12px; justify-content:flex-end; }
      .auth-actions .btn-primary, .auth-actions .btn-outline { padding:10px 14px; border-radius:8px; text-decoration:none; border:none; cursor:pointer; }
      .btn-primary { background:#10b981; color:#fff; }
      .btn-outline { background:transparent; border:1px solid #10b981; color:#10b981; }

      /* Small screens — stack actions and make buttons full width */
      @media (max-width: 480px) {
        .auth-card { padding:16px; margin: 12px; }
        .auth-actions { flex-direction: column; align-items: stretch; }
        .auth-actions .btn-primary, .auth-actions .btn-outline { width:100%; display:block; }
        .auth-title { font-size: 1.125rem; }
      }
    </style>
  </head>
  <body>
    <div class="auth-wrapper">
      <div class="auth-card">
        <div class="card-header">
          <div class="auth-title">Forgot Password</div>
          <div class="auth-sub">Enter your registered email to receive a reset link.</div>
        </div>

        <% if (err) { %>
          <div class="alert alert-error"><%= err %></div>
        <% } %>

        <% if (success) { %>
          <div class="alert alert-success"><%= success %></div>
        <% } %>

        <form class="auth-form" method="POST" action="/forgot-password">
          <div class="form-group">
            <label class="form-label" for="email">Email</label>
            <input id="email" class="form-input" type="email" name="email" placeholder="Enter registered email" required />
          </div>

          <div class="auth-actions">
            <button class="btn btn-primary" type="submit">Send Reset Link</button>
            <a href="/" class="btn btn-outline">Back</a>
          </div>

          <div class="auth-note">If you don't receive an email, check your spam folder or contact support.</div>
        </form>
      </div>
    </div>
  </body>
</html>
